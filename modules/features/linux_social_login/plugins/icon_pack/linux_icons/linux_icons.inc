<?php
/**
 * @file
 * HybridAuth 48px icon pack.
 */

/**
 * Plugin declaration.
 */
$plugin = array(
  'title' => t('Linux Icons'),
  // The below can be commented out if need be to add styling to the icon pack.
  //'css' => 'linux_icons.css',
);
